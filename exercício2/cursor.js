//element.addEventListener('onmouseover',toggleColor(true));
//element.addEventListener('onmouseout',toggleColor(false));

element.addEventListener('mouseover', function(){
    toggleColor(true);
});
element.addEventListener('mouseout', function(){
    toggleColor(false);
});

